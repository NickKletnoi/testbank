name: Deploy Notebook to Databricks
on:
  workflow_dispatch:

jobs:
  push_to_db:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout current repository
        uses: actions/checkout@v3.3.0
      
      - name: Set up python
        uses: actions/setup-python@v4
        with:
          python-version: 3.9

      - name: Databricks CLI config
        run: |
            pip install databricks-cli 
            #curl --request POST \
            #https://dbc-68d2f873-1a1e.cloud.databricks.com/api/2.0/accounts/6a35b239-151a-44ff-997d-c28e1f01381a/oauth2/published-app-integrations \
            #--header 'Content-Type: application/json' \
            #--data '{ "app_id": "bricks-cli" }' \
            #--user "kletnoi@live.com:B1pr0gr@m"
            cd /opt/hostedtoolcache/Python/3.9.16/x64/bin/databricks
            cat > ~/.databrickscfg << EOF 
            [DEFAULT]
            host = https://dbc-68d2f873-1a1e.cloud.databricks.com/
            jobs-api-version = 2.0
            token = dapi8b2754011c4da8f14a50b144f4611477
            EOF 
    
      - name: Deploy code to databricks workspace
        run: |
            databricks workspace import notebooks/test1.py /Users/kletnoi@live.com/testing/test1.py -l python --overwrite
